function tenSS(cb) {
  cb(10);
}

function randomSS(cb) {
  cb(Math.random());
}

/**
 * Exercise: implement merge() for setter-setters
 * and use it to create `tenOrRandom` as a setter-setter.
 */

function merge(xSS, ySS) {
  // return zSS
}
